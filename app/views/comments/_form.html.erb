  <% if !@comment.task %>
    <%= f.label :task_name %>
    <% if !@task %>
     <%= f.collection_select :task_id, Task.all, :id, :task_name %>
     <% end %>
     <% else %>
    <%= f.hidden_field :task_id %>
    <% end %>
<br>
<br>

     <%= f.label :message %>
     <%= f.text_area :message %>
<br>
<br>

    <%= f.submit "Save" %>